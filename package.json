{
    "name": "parcel_example",
    "version": "1.0.0",
    "description": "",
    "scripts": {
        "clean": "rimraf dist",
        "dev": "npm run clean && parcel src/pages/**/*.html",
        "build": "npm run clean && parcel build src/pages/**/*.html --no-source-maps"
    },
    "author": "",
    "license": "ISC",
    "devDependencies": {
        "@parcel/transformer-inline-string": "^2.12.0",
        "parcel": "^2.12.0",
        "rimraf": "^5.0.5"
    }
}
